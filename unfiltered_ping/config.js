// Application Configuration
const config = {
  port: 80,
  networkTimeout: 5000, // 5 seconds
  allowedCommands: ['ping'],
  // TODO: Actually implement input sanitization
};

module.exports = config;